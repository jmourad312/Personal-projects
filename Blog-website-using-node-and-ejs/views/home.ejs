<%- include partials/header.ejs %> 

<h1>Home</h1>
<p><%= homeStartingContent %> </p>

<hr>

<% posts.forEach(post => { %>
    <h3> <%= post.title %>  </h3>
    <% let length = 100;
    let myString = post.body;
    let myTruncatedString = myString.substring(0,length)+"..."+" " %> 
    <p> <%= myTruncatedString %> <a href="/posts/<%= post.title %>">Read more</a></p>
    
    <hr>
<% }); %> 

<%- include("partials/footer") -%> 
